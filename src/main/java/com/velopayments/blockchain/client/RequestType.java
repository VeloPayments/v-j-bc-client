package com.velopayments.blockchain.client;

public enum RequestType {
    TRANSACTION_SUBMISSION,
    LATEST_BLOCK_ID,
    BLOCK_BY_ID,
    BLOCK_ID_BY_HEIGHT,
    TRANSACTION_BY_ID,
    ARTIFACT_BY_ID,
    CONNECTION_STATUS,
}
